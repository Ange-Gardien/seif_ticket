function ValiderForm(nom) {
       
       if (document.formulaire.nom.value.length==0)
           {
           alert('Vous devez saisir le champ Nom : ! ');
           getElementsByClass('nom');
           document.formulaire.nom.focus();
        
           }
       if (formulaire.nom.value.length>10)
           {
           alert('Vous ne pouvez pas saisir plus de 10 caractères.! ');
           getElementsByClass('nom');
           formulaire.nom.focus();
        
           }
       }

  

